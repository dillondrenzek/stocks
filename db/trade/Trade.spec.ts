import chai, { expect } from 'chai';
import mongoose from 'mongoose';
import { withDb } from '../../spec/helpers/db-connect';
import { Trade } from './Trade';

// xdescribe('Trade', withDb(() => {

// }));
