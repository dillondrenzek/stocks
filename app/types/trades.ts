import * as DB from '../../db';

export interface Trade extends DB.ITradeDocument {
  // _id?: string;
  // date: Date;
  // price: number;
  // quantity: number;
  // side: 'buy' | 'sell';
  // symbol: string;
}
