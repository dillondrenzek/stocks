import * as DB from '../db';

export interface Holding extends DB.IHoldingDocument {
  // cost: number;
  // portfolioId: string;
  // quantity: number;
  // symbol: string;
  // tradeIds: string[];
}